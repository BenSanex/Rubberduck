<%= @user.username %>

<%= form_for @question, remote: true, authenticity_token: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, data: {textfield: "title"}%>

  <%= f.label :content %>
  <%= f.text_area :content, data: {send: "message"}%>

  <%= f.submit "Ask" %>
<% end %>
